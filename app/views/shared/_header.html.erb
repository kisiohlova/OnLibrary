<nav class="nav">
  <div class="nav-container">
    <div class="nav-element-container pl-3.5">
      <%= link_to "OnLibrary", books_path, class: "nav-name" %>
      <% if user_signed_in? %>
        <%= link_to "New Book", new_book_path, class: "nav-element" %>
      <% end %>
    </div>
    <div class="nav-element-container">
      <%= render "shared/search_form" %>
      <% if user_signed_in? %>
        <span class="nav-user">Hello, <%= current_user.email %></span>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn-light m-3.5 " %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, class: "btn-light m-3.5" %>
      <% end %>
    </div>
  </div>
</nav>
